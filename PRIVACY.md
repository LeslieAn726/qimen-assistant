# 隐私说明

“每日奇门助手”采用本地优先设计。

## 本地保存的数据

桌面版在 Electron userData 中保存每日记录、普通设置、AI 设置、AI 当日结果和备份。Web 开发模式默认使用项目 `data/`。这些文件不会由应用自动上传或同步。

V2.6 不再读取或创建桌宠设置、陪伴状态和提醒状态。升级前已经存在于 userData 的旧桌宠文件会被原样忽略，不会由应用自动删除。

## AI 请求

AI 功能默认可关闭。只有用户主动生成建议时，服务端才会向用户配置的 OpenAI-compatible Provider 发起请求。当前请求可能包含：当日盘面关键数据、传统规则分析、今日计划和今日重要事项。不会自动读取历史记录、最近若干天数据、备份、黄历或长期记忆。

API Key 保存在独立 `ai-settings.json` 中，仅主进程/Express 服务端读取；前端只得到“是否已配置”的状态。错误信息和正式日志不得包含完整密钥。

第三方 Provider 如何处理数据由其隐私政策决定。若不接受该数据流，请保持 AI 功能关闭。

## 卸载与删除

Windows 卸载程序默认保留 userData，以支持重装后恢复历史。需要彻底删除时，请先在设置页确认并备份必要数据，然后手动删除该应用的 Electron userData 目录。此操作不可恢复。

## 日志

正式安装版不应持续记录用户计划、重要事项、AI Key 或完整用户数据路径。开发模式日志仅用于本机调试，提交问题前仍应检查并脱敏。
