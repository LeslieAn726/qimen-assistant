# 安全政策

## 支持范围

当前维护重点是最新开发版本。安全修复不保证回溯到旧安装包。

## 报告安全问题

请不要在公开 Issue 中提交 API Key、用户记录、绝对路径、备份文件或可直接利用的漏洞细节。仓库启用该功能后，优先通过 GitHub Security Advisory 的“Report a vulnerability”私密报告；如果尚未启用且没有专用安全邮箱，请只公开提交不含敏感细节的联系请求。

报告建议包含：受影响版本、复现步骤、影响范围、最小化日志以及建议修复方式。提交前请移除个人数据和密钥。

## 安全边界

- Renderer 禁止直接访问 Node.js；Electron 保持上下文隔离、关闭 Node 集成并启用沙箱。
- AI API Key 只保存在本地服务端可访问的独立设置文件中，不通过设置读取接口返回。
- 本项目不提供云同步；桌面数据位于 Electron userData。
- 不要运行来源不明的脚本或替换后的二进制依赖。

## 密钥泄露处置

若密钥曾进入提交、日志或截图，应立即在 Provider 控制台撤销并重新生成。仅从当前文件删除并不能消除历史提交中的密钥。
